-- muestra todas las columnas de la tabla users.
SELECT * FROM users;

-- muestra la columna name de la tabla users.
SELECT name FROM users;

-- muestra la columna user_id y name de la tabla users.
SELECT user_id,name FROM users;